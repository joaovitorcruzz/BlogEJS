<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>
    <div class="d-flex justify-content-center" style="margin-top: 12%; ">
        <div style="width: 20%; 
            -webkit-box-shadow: 2px 0px 11px -7px rgba(0,0,0,0.82);
            -moz-box-shadow: 2px 0px 11px -7px rgba(0,0,0,0.82);
            box-shadow: 2px 0px 11px -7px rgba(0,0,0,0.82);
        ">
            <div class="card p-3 pt-0">
                <div class="container mt-2">
                    <div>
                        <h1 class="text-center">Login</h1>
                        <form action="/login/search" method="post">
                            <div class="form-group mb-3">
                                <label for="exampleInputEmail1">Email</label>
                                <input type="text" class="form-control" id="email" name="email"
                                    placeholder="Digite seu email" required>
                            </div>
                            <div class="form-group mb-3">
                                <label for="exampleInputPassword1">Senha</label>
                                <input type="password" class="form-control" id="senha" name="senha"
                                    placeholder="Digite sua senha" required>
                            </div>

                            <% if (typeof errorMessage !== 'undefined') { %>
                                <div style="color:red;" class="mb-2"><%= errorMessage %></div>
                            <% } %>

                            <div class="d-flex justify-content-between">
                                <button type="submit" class="btn btn-primary">Login</button>
                                <button type="submit" class="btn"><a href="#" style="color:rgb(119, 119, 119);">Esqueci a senha</a></button>
                            </div>
                            <div class="d-flex justify-content-center">
                                <button type="submit" class="btn"><a href="/users/add" style="color:rgb(119, 119, 119);">Não tem conta? Clique aqui!</a></button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>